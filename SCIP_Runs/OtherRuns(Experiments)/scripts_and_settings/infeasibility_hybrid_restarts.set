# =============================================================================
# SCIP 10.0 Settings for Infeasibility Proofs
# Variant: HYBRID with BFS-like selection
# =============================================================================
# Best-estimate node selection (BFS-like) for global tree exploration.
# Pure DFS can get trapped; this explores the most promising nodes globally.
#
# Launch:
#   scip.exe -c "set heuristics emphasis off set presolving emphasis aggressive set separating emphasis default set load SCIP_Runs\infeasibility_hybrid_restarts.set read <instance>.lp optimize quit"
# =============================================================================

# ---------------------------------------------------------------------------
# PRESOLVING — individual settings
# ---------------------------------------------------------------------------
presolving/maxrounds = -1
presolving/maxrestarts = -1

# ---------------------------------------------------------------------------
# SEPARATING — MODERATE
# ---------------------------------------------------------------------------
separating/maxrounds = 5
separating/maxroundsroot = 25
separating/maxstallroundsroot = 10

# ---------------------------------------------------------------------------
# CONFLICT ANALYSIS — FULL
# ---------------------------------------------------------------------------
conflict/enable = TRUE
conflict/useinflp = b
conflict/useprop = TRUE
conflict/usepseudo = TRUE
conflict/usesb = TRUE
conflict/maxconss = -1
conflict/maxstoresize = 15000

# ---------------------------------------------------------------------------
# NODE SELECTION — Best-estimate (BFS-like, global view)
# ---------------------------------------------------------------------------
nodeselection/estimate/stdpriority = 100000

# ---------------------------------------------------------------------------
# BRANCHING — Reliability pseudo-cost
# ---------------------------------------------------------------------------
branching/preferbinary = TRUE
branching/relpscost/sbiterquot = 0.5
branching/relpscost/maxreliable = 8.0

# ---------------------------------------------------------------------------
# PROPAGATION — FULL
# ---------------------------------------------------------------------------
propagating/maxrounds = -1
propagating/maxroundsroot = -1
propagating/abortoncutoff = FALSE
propagating/probing/maxprerounds = -1

# ---------------------------------------------------------------------------
# SYMMETRY
# ---------------------------------------------------------------------------
misc/usesymmetry = 7

# ---------------------------------------------------------------------------
# DISPLAY
# ---------------------------------------------------------------------------
display/freq = 100
